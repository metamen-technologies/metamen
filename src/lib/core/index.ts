/**
 * @module @/core
 * Barrel export for core game engine (Layer 1).
 *
 * Convention: named re-exports only.
 * - DO: export { calculateLevel } from './levels/calculate-level';
 * - DON'T: export * from './levels';
 *
 * Subdirectorios existentes en el repo (con .gitkeep):
 * vectors/, levels/, health/, judgement/, economy/,
 * state-machines/, types/, validation/, utils/
 *
 * NOTA: utils/ existe en el repo (creado por scaffolding 02.1.2/02.1.3)
 * aunque 03_tech_spec §4 no lo lista explícitamente.
 * Se mantiene porque el directorio ya está en main.
 *
 * Exports will be added as modules are implemented in Caja 4.
 */

// Placeholder - no modules to export yet
export {};
