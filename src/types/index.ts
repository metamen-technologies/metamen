/**
 * @module @/types
 * Barrel export for global type definitions (Layer 0).
 *
 * Convention: named re-exports only.
 * - DO: export type { UserId } from './user';
 * - DON'T: export * from './user';
 *
 * This is Layer 0 - NO imports from any other internal layer allowed.
 * Exports will be added as types are defined.
 */

// Placeholder - no types to export yet
export {};
